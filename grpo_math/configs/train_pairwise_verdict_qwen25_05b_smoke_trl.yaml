seed: 1234

model:
  name_or_path: "Qwen/Qwen2.5-0.5B-Instruct"
  torch_dtype: "float32"
  use_flash_attn: false

data:
  source: "pairwise_jsonl"
  jsonl_path: "outputs/pairwise_rollouts_debug/samples_llama31_8b_vllm_single_verify_fast.jsonl"
  split_train: "train"
  split_eval: "eval"
  eval_fraction: 0.2

prompt:
  template: |
    Question:
    {question}

    Solve step by step in plain text only.
    Keep your response concise.
    Your last line must be exactly:
    FINAL_ANSWER: <integer>

reward:
  mode: "verdict"
  teacher:
    verify_prompt_template_path: "grpo_math/prompts/single_solution_verify_prompt.txt"
    api_model: "meta-llama/Llama-3.1-8B-Instruct"
    api_base_url: "http://127.0.0.1:8001/v1"
    api_key: "EMPTY"
    api_max_tokens_param: "max_tokens"
    api_timeout_s: 180
    api_max_retries: 8
    api_backoff_initial_s: 0.5
    temperature: 0.0
    top_p: 1.0
    max_new_tokens: 128

rollout:
  k: 2
  temperature: 0.7
  top_p: 0.95
  max_new_tokens: 1536

train:
  output_dir: "outputs/trl_grpo_pairwise_verdict_qwen25_05b_smoke"
  steps: 1
  prompts_per_step: 4
  grad_accum_steps: 1
  lora:
    enabled: true
    r: 16
    alpha: 32
    dropout: 0.05
    bias: "none"
    target_modules: "all-linear"

  lr: 1.0e-5
  warmup_steps: 0
  kl_beta: 0.01

  gradient_checkpointing: false
  eval_every: 0
  save_every: 1

  wandb:
    enabled: true
    project: "grpo-math"
    run_name: "trl-grpo-pairwise-verdict-qwen25-05b-smoke"

  debug_rollouts:
    enabled: false
